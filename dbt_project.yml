name: ragany2
version: '1.0'
config-version: 2

profile: transporters_profile

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
data-paths: ["data"]
macro-paths: ["macros"]

# Default materialization for models
models:
  ragany2:
    +materialized: view
